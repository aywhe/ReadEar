- 创建tts总是失败，status = -1
override fun onInit(status: Int) {
        Log.d("ContentActivity", "TTS 初始化状态：$status")
        if (status == TextToSpeech.SUCCESS) {...}
解决办法：https://cloud.tencent.com/developer/article/2516111
<queries>
    <intent>
        <action android:name="android.intent.action.TTS_SERVICE" />
    </intent>
</queries>
- textToSpeech的回调函数不会触发，比如下面的onStart、onDone、onError等
textToSpeech?.setOnUtteranceProgressListener(object : android.speech.tts.UtteranceProgressListener() {
                  override fun onStart(utteranceId: String?) {...}
解决办法： https://blog.csdn.net/u010555997/article/details/50926570
speak第四个参数不要是null，就可以触发了。